# TitleBarHarmony3267

- **Pixso**: [OPXcaxT2Rj_9QE_M8xckbw — item `3267:13296`](https://pixso.cn/app/design/OPXcaxT2Rj_9QE_M8xckbw?item-id=3267:13296)
- **用途**: 设置页风格标题栏：左侧返回圆形按钮、中间主副标题、右侧最多三个圆形图标按钮。
- **说明**: MCP `design_to_code` 对该节点返回的是包含多行变体的整段画板；本组件实现其中**首行标准布局**（导出结构中的 `stategroup-3267_13297`）。样式与现有 `TitleBar` 的 Harmony 几何一致；Pixso 导出 CSS 未落盘（批次 URL），见 `.cursor/skills/pixso-to-shadcn-react/SKILL.md`。
- **Props**: `title` / `subtitle`、`leftIcon`、`onLeftClick`、`rightIcons`、`onRightIconClick`、`left` / `right` 插槽、`backgroundColor`、`className`。
